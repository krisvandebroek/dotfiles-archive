- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.inputrc: inputrc
    ~/.bash_prompt: bash_prompt 
    ~/.aliases: aliases
    ~/.maven-aliases: maven-aliases
    ~/.exports: exports
    ~/.functions: functions
    ~/.vimrc: vimrc
    ~/.vim: vim/
    ~/.git-completion: git-completion
    ~/.mvn-completion: maven-bash-completion/bash_completion.bash

- shell:
  - git update-submodules # after linking ~/.gitconfig
